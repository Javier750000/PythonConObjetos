program patito;
var
   float: b, c;
   int: a, i;


func int uno(int aba, int hola)
var
   int: x;
{
   x=1;
   print(x);
   return (x);
}

func float doble(float z)
{
   return (2.0);
}


main()
{
   i = 0;
   print("Ingrese el valor de la variable a.");
   read(a);
   if (a > 5)
   {
      a = 5;
   }
   while (i < a) {
      print("print while");
      i = i+1;
   }
   print(uno(2, 1));
   print(doble(1.0));
   print("Este es el nuevo valor de la variable a.");
   print(a);
}
